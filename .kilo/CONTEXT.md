# Project context

- Repository/workspace root: `/home/jitto/Projects/dotodo-genx/DoToDo2-kilo`
- Ionic app root: `/home/jitto/Projects/dotodo-genx/DoToDo2-kilo/ionic-app`
- Ionic app package manifest: `/home/jitto/Projects/dotodo-genx/DoToDo2-kilo/ionic-app/package.json`
- Run Ionic app commands from the `ionic-app` directory, not from the repository root.
- Node and npm are installed through nvm. Before running Node/npm scripts, load nvm first, for example:
  `source "$HOME/.nvm/nvm.sh" && cd /home/jitto/Projects/dotodo-genx/DoToDo2-kilo/ionic-app && npm run build`
- Useful Ionic commands:
  - `npm run build` for TypeScript and Vite build validation.
  - `npm run dev` for the Vite dev server.
  - `npm run lint` for linting.

